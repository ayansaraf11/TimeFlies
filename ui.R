
library(shiny)

# Define UI for application that draws a histogram
shinyUI(fluidPage(
  
  # Application title
  titlePanel("TimeFlies"),
  
  sidebarLayout(
    sidebarPanel(),
    
    mainPanel(
    )
  )
))
